.row
  .span10.offset1.day
    %h2 What happened on #{@month}/#{@day}
    - @cards.each do |card|
      .entry[card]
        .year
          = card.year
          .dow= day_of_the_week_for(card)
        .text= card.entry
        - if card.rotation.present?
          Rotation: #{Card::SHIFTS[card.rotation.to_sym]}.
          %br
          Shift: #{card.time_in.strftime("%b %e %l:%M %P")} - #{card.time_out.strftime("%b %e %l:%M %P")}
          %br
          Notes Duration: #{card.notes_duration}
