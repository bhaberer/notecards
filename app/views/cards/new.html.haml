.content
  - unless current_user.has_done_yesterdays_card?
    .alert
      %button.close{ :type => :button, 'data-dismiss' => :alert } &times;
      %strong Last chance!
      Hey, you didn't fill out a card yesterday, but you can still fill it out until tomorrow!
      = link_to 'Fill out Yesterday\'s Card', yesterday_path, :class => 'btn btn-info btn-mini pull-right'
  .entry
    - if current_user.has_done_todays_card?
      .alert.alert-block.alert-success
        %strong Well done!
        %p
          You've submitted today's notecard, remember to come back tomorrow!
        %p
          If you made a mistake or need to update today's entry head to your cardbox,
          you have 48 hours to make any edits nessesary.
        %br
        = link_to 'Cardbox', profile_path(current_user.username),
                  :class => 'btn btn-info centered-text'
        - if current_user.vet?
          = link_to 'Track your encounters in CTS',
                    'https://secure.vetmed.ucdavis.edu/Curriculum/CTS/default.cfm?Page=MyClinicalEncounters',
                    :class => 'btn btn-info centered-text'

    - else
      = render 'form'
