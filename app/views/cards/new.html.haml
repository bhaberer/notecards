.content
  - unless current_user.has_done_yesterdays_card?
    .alert.alert-block
      %button.close{ :type => :button, 'data-dismiss' => :alert } &times;
      %h4 Last chance!
      %p Hey, you didn't fill out a card yesterday, but you can still fill it out until tomorrow!
      %br
      = link_to 'Fill out Yesterday\'s Card', yesterday_path, :class => 'btn btn-info centered-text'
  .entry
    - if current_user.has_done_todays_card?
      .alert.alert-success
        %strong Well done!
        You've submitted today's notecard, remember to come back tomorrow!
    - else
      = render 'form'
  #card
    - if @cards.present?
      = render :partial => 'card'
