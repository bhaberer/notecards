.form 
  = form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f|
    = devise_error_messages!
    .field
      = f.label :email
      %span Conformation Email
    = f.submit "Resend", :class => 'submit'
